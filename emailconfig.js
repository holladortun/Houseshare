const servicekey = import.meta.env.VITE_APP_EMAIL_SERVICE_KEY;

const templateid = import.meta.env.VITE_APP_EMAIL_TEMPLATE_ID;

export { servicekey, templateid };
console.log(servicekey, templateid);
